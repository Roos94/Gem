<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dtu.smmac.gem">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Startskaerm"
            android:label="@string/app_name"
            android:theme="@style/SK"/>
        <activity android:name=".NyReg"
            android:theme="@style/SK"/>
        <activity
            android:name=".Hovedskaerm"
            android:theme="@style/SK" />
        <activity android:name=".Datering" />
        <activity android:name=".Skabelon" />
        <activity android:name=".Beskrivelse" />
        <activity android:name=".Emnegruppe" />
        <activity android:name=".Referencer" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".Betegnelse" />
        <activity android:name=".Modtagelsesdato"></activity>
    </application>

</manifest>
